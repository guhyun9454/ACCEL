제공된 문서에 소개된 PriDe (Debiasing with Prior Estimation) 방법은 대규모 언어 모델(LLM)이 객관식 문제(MCQ)에서 특정 선택지(예: A 또는 C)를 선호하는 '선택 편향(Selection Bias)'을 해결하기 위해 제안된 기법입니다.
이 방법의 핵심은 모델의 편향(Prior)을 실제 지식(Debiased belief)과 분리하여 추정하고, 이를 통해 효율적으로 예측을 보정하는 것입니다.
상세한 작동 원리와 절차는 다음과 같습니다.
1. 핵심 아이디어: 확률의 분해 (Prediction Probability Decomposition)
PriDe는 모델이 관찰하는 예측 확률($P_{observed}$)이 '모델의 선택지 ID에 대한 사전 편향($P_{prior}$)'과 '내용에 대한 실제 지식($P_{debiased}$)'의 곱으로 구성된다고 가정합니다1.

즉, 모델이 특정 답(예: A)을 선택할 확률은 다음과 같이 표현됩니다.

$$P_{observed}(d_i|q, x^I) = Z^{-1} P_{prior}(d_i|q) P_{debiased}(o_{f_I(i)}|q, x)$$
여기서 $d_i$는 선택지 ID(A, B, C, D 등), $o$는 실제 선택지 내용, $q$는 질문입니다. 이 식은 모델이 정답을 맞히려는 실제 능력과 상관없이, 단순히 'A'라는 기호를 선호하는 편향이 결과에 영향을 미친다는 것을 수학적으로 모델링한 것입니다2.

2. PriDe 알고리즘의 작동 단계
PriDe는 전체 데이터에 대해 비용이 많이 드는 순열(Permutation) 방식을 적용하는 대신, 소수의 샘플만 사용하여 편향을 추정한 뒤 나머지 데이터에 적용하여 효율성을 극대화합니다3.

구체적인 단계는 다음과 같습니다.
1단계: 편향 추정을 위한 샘플링 (Prior Estimation)
전체 테스트 데이터 중 매우 적은 비율(예: 5% 또는 $K$개)의 샘플($\mathcal{D}_e$)을 추출합니다4.

2단계: 샘플에 대한 편향 계산
추출된 샘플들에 대해서는 선택지의 순서를 바꾸는 '순환 순열(Cyclic Permutation)' 방식을 적용하여 모델의 추론을 수행합니다. 선택지 내용의 순서가 바뀌더라도 모델이 여전히 특정 ID(예: A)에 높은 확률을 부여한다면, 이는 모델의 고유한 편향(Token bias)입니다.
이 과정에서 레이블(정답) 없이도 다음 식을 통해 각 질문에 대한 편향($P_{prior}$)을 역산할 수 있습니다5.


$$P_{prior}(d_i|q) = \text{softmax}\left(\frac{1}{|\mathcal{I}|}\sum_{I \in \mathcal{I}} \log P_{observed}(d_i|q, x^I)\right)$$
3단계: 전역 편향 도출 (Global Prior)
앞서 계산한 개별 샘플들의 편향을 평균내어, 해당 도메인이나 모델 전체에 적용할 수 있는 '전역 편향($\tilde{P}_{prior}$)'을 구합니다. 연구진은 모델의 편향이 도메인 간에 유사하게 나타나므로(transferable), 소수의 샘플로도 전체 편향을 잘 근사할 수 있음을 확인했습니다6666.
+1

4단계: 효율적인 보정 (Inference & Debiasing)
나머지 대다수의 데이터($\mathcal{D}_r$)에 대해서는 추가적인 순열 연산 없이, 3단계에서 구한 전역 편향을 사용하여 즉시 보정합니다. 관찰된 확률을 편향 확률로 나누어주는 방식입니다7.

최종 보정된 확률은 다음과 같습니다.

$$\tilde{P}_{debiased}(o_i|q, x) \propto P_{observed}(d_i|q, x) / \tilde{P}_{prior}(d_i)$$
3. PriDe의 주요 장점
높은 효율성 (Efficiency): 모든 문제에 대해 선택지 순서를 섞어서 여러 번 추론하는 기존의 순열 기반 방식(Permutation-based debiasing)은 비용이 $N$배(선택지 개수만큼) 들지만, PriDe는 극소수의 샘플만 순열 연산을 수행하므로 추가 연산 비용이 거의 없습니다(negligible overhead)8888.
+1


해석 가능성 (Interpretability): 추정된 Prior 값은 모델이 특정 ID(A, B, C 등)를 얼마나 선호하는지를 명시적으로 보여주므로, 모델의 편향을 해석하는 데 도움을 줍니다9999.
+1


비지도 방식 (Label-free): 정답 레이블이 필요 없는 추론 단계(inference-time)의 방법이므로, 정답을 모르는 실제 테스트 상황에서도 바로 적용할 수 있습니다10101010.
+1


결론적으로 PriDe는 LLM이 내용을 보지 않고 "A가 정답일 거야"라고 찍는 **토큰 편향(Token bias)**을 수학적으로 발라내어, 모델이 실제 지식에 기반해 정답을 고르도록 돕는 효율적인 알고리즘입니다.
